# setup.py

from setuptools import setup, find_packages

setup(
    name='automl',
    version='0.1',
    packages=[
        'automl',
    ],
    install_requires=[
        # 'openai>=1.51.0',
        'plotly',
    ]
)
